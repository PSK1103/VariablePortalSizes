main: info.gomeow.portalsizes.PortalSizes
name: VariablePortalSizes
version: ${project.version}

author: gomeow

permissions:
  variableportalsizes.*:
    children:
      variableportalsizes.abnormal: true
  variableportalsizes.abnormal:
    default: true